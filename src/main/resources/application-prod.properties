# ========================
# Database (AWS RDS MySQL)
# ========================
spring.datasource.url=jdbc:mysql://database-2.cslcosiiehnu.us-east-1.rds.amazonaws.com:3306/moviesdb
spring.datasource.username=admin
spring.datasource.password=Swasti123456

# ========================
# Hibernate / JPA
# ========================
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# ========================
# Eureka Client (Service Discovery)
# ========================
eureka.client.service-url.defaultZone=http://<EC2_PUBLIC_IP>:8761/eureka/

# Prefer IP instead of hostname to avoid UnknownHostException
eureka.instance.prefer-ip-address=true

# Optional: explicitly register with EC2 IP (good for stable deployment)
# eureka.instance.ip-address=<EC2_PUBLIC_IP>

# ========================
# App Settings
# ========================
spring.application.name=movie-service
server.port=8081

# ========================
# Logging
# ========================
logging.level.org.springframework=INFO
